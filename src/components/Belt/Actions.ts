export enum BeltDressingWindowActions {
    LOAD_CONTENT,
    CREATE_NEW_SET,
    SAVE_SET,
    REMOVE_SET,
    SELECT_SET,

    UNEQUIP_ITEM,
    UNEQUIP_ALL,
    EQUIP,
    EQUIP_DND,
    EQUIP_FROM_SET,

    ADD_FILTER,
    REMOVE_FILTER
}